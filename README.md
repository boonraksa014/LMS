
  # LMS

  This is a code bundle for LMS. The original project is available at https://www.figma.com/design/ar0W69QcwnLrtTKxaJtwUD/LMS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  