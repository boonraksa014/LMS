# Copy this file to .env and fill in your values
# When VITE_API_URL is set to a non-localhost URL, all services will call the real backend.
# When unset or pointing to localhost, the app uses mock/localStorage data.

VITE_API_URL=https://your-backend.com/api